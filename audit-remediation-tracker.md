# Audit Remediation Tracker

Sources used: `audit.txt`, the follow-up messages you provided, local git history in this repo, and upstream Blobstream PR [celestiaorg/blobstream-contracts#428](https://github.com/celestiaorg/blobstream-contracts/pull/428).

Note on commit ancestry: the report revision shown in the PDF text is `Rev. 485288e`, but that does not resolve to a Git commit in this repository. In the last column below, ancestry is therefore described from the actual direct parent(s) of the cited fix commit(s) in this repo.

| Finding | Fix Commit(s) or ACK | Additional comments for the report (these may be quoted or paraphrased in the relevant remediation section of the report) | Are the fix commit’s direct ancestors only the audit commit or other fix commits? If not, why not? |
| --- | --- | --- | --- |
| 3.1. Continuation read proof does not check the real payload size | `5c4d1cfaa6873c5b1c6300aefbc7bd41de86e933` (primary). Later coverage follow-up: `f6d3e837dad158bd2621a6cda3c38aebeaa07b94`. | Celestia Labs stated that the proof system was updated to verify the first share together with continuation shares so the contract can check the true `payloadSize`. Later tests broadened payload-size-proof coverage. | No. `5c4d1cf` is stacked on earlier remediation work; its direct parent is `6cf6521a152197e4d956066af7014ccec0a0bbe9`. The later coverage commit `f6d3e83` is also stacked on follow-up remediation commits. |
| 3.2. Read preimage proof does not bind proved shares to the share picked by the certificate | `41269136126446265d88d96d8e3dfa3faee593b3` (primary). Follow-up hardening: `ba32363cf86c48eedcf87fb1716009602a64b7b9`. | Celestia Labs stated that `validateReadPreimage` was updated to check share-position binding. A later follow-up corrected row-width handling in `_validateProvedSharePositions`, and additional tests were added around wrong-share and wrong-position cases. | No. `4126913` is not isolated on top of the audited snapshot; its direct parent is `5c4d1cf`, another remediation commit. `ba32363` is a later follow-up on the same remediation stack. |
| 3.3. Validating the certificate depends on BlobstreamX.verifyAttestation terminating | ACK in this repo. Upstream fix merged in Blobstream: [celestiaorg/blobstream-contracts#428](https://github.com/celestiaorg/blobstream-contracts/pull/428) (`b8482c59cf758ee4566436d2c3564394c5a163ca`; head `6bbacc4629176031dca3c54868c56ae00da84ebb`). | Celestia Labs stated they understood the concern and that Blobstream is intended to handle adversarial verification inputs safely. They also reported the potential infinite-loop issue upstream. The upstream PR adds an early bound check so `numLeaves > 2^255` returns `false` rather than looping or reverting. Report wording should note that remediation depends on running a Blobstream deployment that includes this upstream fix. | `N/A` for this repo: the remediation is upstream, not a local `nitro-das-celestia` commit. Also note that upstream rollout may require upgrading already deployed Blobstream proxies, not just merging the code. |
| 3.4. The function validateCertificate may return false for a valid proof under some gas limits | `46a0f09e9b10d6665cc96170cfae05f310f234b6`, `047b13d50f97401ae8a87c2399d89483f73b77dd` | Celestia Labs stated that `decodeAttestation` was rewritten to avoid the external self-call / `try-catch` pattern and related revert behavior. They also noted that malformed proof offsets are now handled explicitly, including `tupleOffset + 128`, `proofOffset = tupleOffset + proofOffsetRel`, `sideNodesStart = proofOffset + sideNodesOffset`, and `sideNodesLen * 32`. | No. These are stacked fixes. `46a0f09` directly descends from `4126913`, and `047b13d` directly descends from `46a0f09`. |
| 3.5. Unsafe uint64 to big.Int conversion | ACK / open. No matching remediation commit identified in this repo. | The report still shows this item as `TBD`, and current code still contains `big.NewInt(int64(...))` conversions in `daserver/proof.go` and `daserver/celestia.go`. The report should either keep this item open or note that a follow-up is still required to switch to `new(big.Int).SetUint64(...)`. | `N/A` because no repository fix commit was identified for this specific issue. |
| 3.6. Missing retry logic on postsubmission read operations | ACK / open. No matching remediation commit identified in this repo. | The report still shows this item as `TBD`, and current `Store` logic still performs post-submit `Blob.Get` and `Header.GetByHeight` without the `retryWithBackoff` wrapper used elsewhere. This looks unresolved on the current branch. | `N/A` because no repository fix commit was identified for this specific issue. |
| 3.7. Possible infinite loop in backwards search in filter() | `989e035742230ecbf092f01141838aae5fcaa371` | The `blobstream event` refactor now terminates once the search window reaches zero instead of rescanning the same low block range indefinitely. The function returns `nil` when no matching event exists, which aligns better with the invalid-certificate classification expected for missing Blobstream events. | No. `989e035` is stacked on earlier work; its direct parent is `f153b2aedafcbe1dfd11d205e67501ffd62fcf48` (`fix: test coverage`). |
| 3.8. Noncompliant error messages for invalid certificate errors in reader methods | `6cf6521a152197e4d956066af7014ccec0a0bbe9` | Celestia Labs acknowledged that read-path and read-preimage validator-path functions were leaking low-level parsing errors directly. The fix wraps malformed and parsing-related invalid-certificate cases with `certificate validation failed`, matching the Nitro DA integration contract. | No. `6cf6521` is not a one-commit delta from an identified audit snapshot; its direct parent is `75d6de3b605ec2341ab2e331ddcb71a5207c5456`, and later related fixes are stacked above it on the same branch. |
| 3.9. Noncompliant error in the case of an empty batch | `653b0fe3ef88abf7e8fc63bd5ce6d90967bf6663` | Celestia Labs confirmed that `recoverPayload` and `collectPreimages` should handle empty batches successfully. The fix removes the early `empty payload returned from Celestia` error path so empty batches are treated as success rather than as infrastructure failures. | No. `653b0fe` is stacked; its direct parent is `0808b69795c9f7891c074ecd4045752ca815f69d`, another remediation-era commit. |
| 3.10. Insufficient test coverage | `f153b2aedafcbe1dfd11d205e67501ffd62fcf48` (partial), plus targeted tests in later fix commits | Test coverage was expanded substantially, especially in `daserver/blobstream_test.go`, `daserver/max_message_size_test.go`, and `daserver/types/reader_test.go`, and later remediation commits add finding-specific Solidity and reader tests. That said, the report’s specific note about the untested `daserver/types/tree/` package still appears open on the current branch. | Mixed / not fully attributable. `f153b2a` is not stacked on another cited audit-fix commit; its direct parent is `0872b54ed1d93d8c866266a68111bbf53b97044b`. Because the report revision is not a repo commit, exact “audit commit only” ancestry cannot be proven. |
| 4.1. Potential memory-corruption risk in prefix slice reuse | ACK / advisory only. No fix commit identified. | The report itself frames this as defensive future-proofing rather than a current bug. Current code still has the TODO about giving the prefix slices larger capacity, so if that TODO is ever acted on, the append pattern should be hardened at the same time. | `N/A` because no fix commit was identified and the item is advisory. |
| 4.2. Unused timeout parameter in writerForCelestia.Store RPC method | ACK, with related cleanup in `0b6cf180a9f15d88d0438d646b0df990f4671d2e` | Celestia Labs stated that `Store(timeout)` intentionally accepts the parameter only to satisfy the Nitro interface, because Celestia does not yet support blob lifetimes. They also clarified that this timeout is about expiry / lifetime, not request duration. The related cleanup commit removes the previous request-timeout interpretation so the code better matches that intent. | Not purely on top of an identified audit snapshot. The related cleanup commit `0b6cf18` directly descends from `fcab01120228cb68ef9d86cf616e9acd78d1688d`; this is branch history rather than a standalone audit-only delta. |
| 4.3. Silent validation failure in GetProof | ACK / appears open. No matching remediation commit identified for the legacy `GetProof` path. | The report flags this as a legacy-interface discussion item. Current `GetProof` still returns `nil, err` after `valid == false`, which effectively means `nil, nil` on that path because `err` is unset there. If Celestia Labs decides this behavior should change, it still looks actionable on the current branch. | `N/A` because no repository fix commit was identified for this specific issue. |
| 4.4. Iterator error handling causes silent discard of a valid event in filter | ACK / appears open. No matching remediation commit identified for the specific close-error precedence issue. | Although `989e035` reworked `filterBlobstreamCommitmentEvents` for the infinite-loop / missing-event behavior, the current code still checks `eventsIterator.Error()` and `Close()` before returning a found match. That means a found event can still be discarded if iterator finalization reports an error. This discussion item appears unresolved. | `N/A` because no repository fix commit was identified for this specific issue. |
